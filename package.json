{
  "name": "trust-badger",
  "version": "0.2.0",
  "description": "Context-aware runtime enforcement for AI agents in CI/CD",
  "main": "src/setup.js",
  "scripts": {
    "build": "ncc build src/setup.js -o dist && ncc build src/proxy.js -o dist/proxy-bundle && cp dist/proxy-bundle/index.js dist/proxy.js && rm -rf dist/proxy-bundle",
    "test": "node --test tests/policies.test.js tests/proxy.test.js tests/clinejection.e2e.test.js"
  },
  "keywords": ["github-action", "security", "mcp-proxy", "ai-agent", "ci-cd", "runtime-enforcement"],
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
